{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@touca/fbs-schema",
  "version": "0.0.0",
  "description": "Touca FlatBuffers Schema",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "lint": "prettier --write src/",
    "postinstall": "npm run build"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "prettier": "^2.7.1",
    "ts-node": "^10.8.1",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "flatbuffers": "^2.0.6"
  }
}
