{
  "private": true,
  "name": "@touca/examples",
  "version": "0.0.0",
  "devDependencies": {
    "lerna": "^4.0.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.3.5"
  },
  "scripts": {
    "build": "lerna run --stream --concurrency 1 --sort build",
    "clean": "lerna run --parallel clean && lerna clean --yes"
  }
}
