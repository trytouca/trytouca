{
  "private": true,
  "name": "@touca/examples",
  "version": "0.0.0",
  "scripts": {
    "build": "lerna run --stream --concurrency 1 --sort build",
    "clean": "lerna run --parallel clean && lerna clean --yes"
  },
  "workspaces": [
    "01_node_minimal",
    "02_node_main_api",
    "03_node_core_api"
  ],
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^16.7.10",
    "lerna": "^4.0.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.4.2"
  }
}
