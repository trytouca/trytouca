{
  "private": true,
  "name": "@touca/examples",
  "version": "0.0.0",
  "scripts": {
    "build": "lerna run --stream --concurrency 1 --sort build",
    "clean": "lerna run --parallel clean && lerna clean --yes"
  },
  "workspaces": [
    "01_node_minimal",
    "02_node_basic_api",
    "03_node_client_api"
  ],
  "dependencies": {
  },
  "devDependencies": {
    "@types/node": "^16.6.0",
    "lerna": "^4.0.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.3.5"
  }
}
